# Mercury tests
#
# Phase 1 (v1.0.0): placeholder.
#
# Planned test coverage:
#   test_commands.py     — CLI command dispatch and argument parsing
#   test_node_mock.py    — Node API calls with mocked LND/bitcoind
#   test_mm_post.py      — Mattermost webhook posting
#   test_grok_fetch.py   — Playwright extraction (with recorded fixtures)
